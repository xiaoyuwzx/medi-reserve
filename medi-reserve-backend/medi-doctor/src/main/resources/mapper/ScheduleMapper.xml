<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.medireserve.doctor.mapper.ScheduleMapper">

    <!-- 根据医生ID和日期范围查询排班列表（动态SQL） -->
    <select id="findByDoctorIdAndDateRange" resultType="Schedule">
        select *
        from schedule where doctor_id = #{doctorId}
        <if test="startDate != null">
            and schedule_date &gt;= #{startDate}
        </if>
        <if test="endDate != null">
            and schedule_date &lt;= #{endDate}
        </if>
        <if test="status != null">
            and status = #{status}
        </if>
        order by schedule_date asc , period asc
    </select>

    <!--
        =============================================================
        获取历史就诊率（过去4周同一天）
        =============================================================

        就诊率 = 实际就诊数 / 总号源数

        示例：医生张三，过去4个周三：
          - 第1个周三：放号20，就诊18 → 就诊率 90%
          - 第2个周三：放号20，就诊16 → 就诊率 80%
          - 第3个周三：放号20，就诊19 → 就诊率 95%
          - 第4个周三：放号20，就诊17 → 就诊率 85%
          平均就诊率 = (90+80+95+85)/4 = 87.5% → 触发加号

        为什么用「就诊数」而不是「预约数」？
        - 预约数包含了「预约了但没来」的爽约患者
        - 就诊数（status=1 或 2）才是真正消耗了医疗资源的
        - 用就诊数计算更准确，避免放号过多导致医生空等
    -->
    <select id="getHistoricalOccupancyRate" resultType="java.lang.Double">
        select
            ifnull(sum(a.cnt), 0) / nullif(sum(s.max_count), 0) as occupancy_rate from schedule s
        left join (
            select schedule_id, count(*) as cnt from appointment
            where status in (1, 2)  <!-- 1=已支付, 2=已就诊（有效挂号） -->
            group by schedule_id
        ) a on s.id = a.schedule_id
        where s.doctor_id = #{doctorId}
        <!-- Java: 1=周一, 7=周日; MySQL: WEEKDAY: 0=周一, 6=周日 -->
        and weekday(s.schedule_date) = (#{dayOfWeek} - 1)
        and s.schedule_date between date_sub(#{targetDate}, interval 4 week) and date_sub(#{targetDate}, interval 1 day)
    </select>

</mapper>