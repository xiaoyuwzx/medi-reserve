<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.medireserve.patient.mapper.EvaluationMapper">

    <!--插入评价记录-->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into evaluation (appointment_id,
                                patient_id,
                                doctor_id,
                                schedule_id,
                                score,
                                content,
                                is_anonymous,
                                status)
        values (#{appointmentId},
                #{patientId},
                #{doctorId},
                #{scheduleId},
                #{score},
                #{content},
                #{isAnonymous},
                1 /*默认已发布*/)
    </insert>

    <!--
        =============================================================
        热门医生排行榜（使用时间衰减算法）
        =============================================================

        核心公式：hot_score = AVG(score * 0.98^(今天 - 评价日期) )

        公式拆解：
        1. score：本次评价的原始评分（1-5星）
        2. 0.98：衰减系数（每天衰减 2%）
        3. (今天 - 评价日期)：评价过去的天数（如 30 天前 = 30）
        4. 0.98^30 ≈ 0.545（30天前的评价权重只剩 54.5%）
        5. 1天前的评价权重 = 0.98^1 = 98%（几乎不衰减）

        设计亮点：
        - 只统计近 30 天的评价（更关注近期口碑）
        - 使用 AVG 聚合（不是 SUM，避免评价多的医生绝对占优）
        - 按 hotScore 降序排列（热度越高越靠前）

        为什么不直接用「近30天平均分」？
        - 近30天平均分：今天和30天前的评价权重一样
        - 时间衰减：今天的评价权重是98%，30天前的是54.5%
        - 更能反映「最近趋势」而非「历史平均」
    -->
    <select id="findHotDoctors" resultType="com.medireserve.common.dto.DoctorHotVO">
        select
        e.doctor_id as doctorId,
        d.name as doctorName,
        dept.name as departmentName,
        t.name as titleName,
        da.avatar as avatar,
        round(avg(e.score * pow(0.98, datediff(now(), e.created_at))), 2) as hotScore,
        count(*) as evaluationCount
        from evaluation e
        inner join doctor d on e.doctor_id = d.id
        inner join doctor_audit da on d.id = da.doctor_id and da.audit_status = 1
        left join department dept on d.department_id = dept.id
        left join title t on d.title_id = t.id
        where e.status = 1 and e.created_at > DATE_SUB(now(), interval 30 day)
        group by e.doctor_id, d.name, dept.name, t.name, da.avatar
        order by hotScore desc
        limit #{limit}
    </select>

    <!--查询患者的所有评价-->
    <select id="findByPatientId" resultType="com.medireserve.common.dto.MyEvaluationVO">
        select
            e.id as evaluationId,
            e.doctor_id as doctorId,
            d.name as doctorName,
            dept.name as departmentName,
            e.score,
            e.content,
            e.is_anonymous = 1 as isAnonymous,
            e.status,
            e.created_at as createdAt
        from evaluation e
        inner join doctor d on e.doctor_id = d.id
        left join department dept on d.department_id = dept.id
        where e.patient_id = #{patientId}
        order by e.created_at desc
    </select>

    <!--查询某医生的评价列表-->
    <!-- 只查询已发布的评价，匿名评价不显示患者姓名 -->
    <select id="findByDoctorId" resultType="com.medireserve.common.dto.EvaluationListVO">
        select
            e.id as evaluationId,
            e.doctor_id as doctorId,
            d.name as doctorName,
            dept.name as departmentName,
            s.schedule_date as scheduleDate,
            e.score,
            e.content,
            case when e.is_anonymous = 1 then 1 else 0
            end as isAnonymous,
            e.created_at as createdAt
        from evaluation e
        inner join doctor d on e.doctor_id = d.id
        left join department dept on d.department_id = dept.id
        left join schedule s on e.schedule_id = s.id
        where e.doctor_id = #{doctorId} and e.status = 1
        order by e.created_at desc
    </select>

</mapper>