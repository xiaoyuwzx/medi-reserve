<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.medireserve.admin.mapper.AdminDoctorManageMapper">

    <!-- 结果映射 -->
    <resultMap id="AdminDoctorVOMap" type="com.medireserve.common.dto.AdminDoctorVO">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="phone" column="phone"/>
        <result property="idCard" column="id_card"/>
        <result property="gender" column="gender"/>
        <result property="departmentId" column="department_id"/>
        <result property="departmentName" column="departmentName"/>
        <result property="titleId" column="title_id"/>
        <result property="titleName" column="titleName"/>
        <result property="specialty" column="specialty"/>
        <result property="introduction" column="introduction"/>
        <result property="avatar" column="avatar"/>
        <result property="status" column="status"/>
        <result property="auditStatus" column="auditStatus"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <!-- 分页查询医生账号列表 -->
    <select id="findDoctorList" resultMap="AdminDoctorVOMap">
        SELECT
        d.id,
        d.name,
        d.phone,
        d.id_card,
        d.gender,
        d.department_id,
        dept.name AS departmentName,
        d.title_id,
        t.name AS titleName,
        da.specialty,
        da.introduction,
        da.avatar,
        d.status,
        da.audit_status AS auditStatus,
        d.created_at,
        d.updated_at
        FROM doctor d
        LEFT JOIN department dept ON d.department_id = dept.id
        LEFT JOIN title t ON d.title_id = t.id
        LEFT JOIN doctor_audit da ON d.id = da.doctor_id
        <where>
            <!-- 只显示已审核的医生 -->
            AND da.audit_status = 1
            <if test="q.keyword != null and q.keyword != ''">
                AND (d.name LIKE CONCAT('%', #{q.keyword}, '%')
                OR d.phone LIKE CONCAT('%', #{q.keyword}, '%'))
            </if>
            <if test="q.departmentId != null">
                AND d.department_id = #{q.departmentId}
            </if>
            <if test="q.status != null">
                AND d.status = #{q.status}
            </if>
            <if test="q.auditStatus != null">
                AND da.audit_status = #{q.auditStatus}
            </if>
        </where>
        ORDER BY d.created_at DESC
    </select>

    <!-- 统计符合条件的医生总数 -->
    <select id="countDoctorList" resultType="long">
        SELECT COUNT(*)
        FROM doctor d
        LEFT JOIN doctor_audit da ON d.id = da.doctor_id
        <where>
            da.audit_status = 1
            <if test="q.keyword != null and q.keyword != ''">
                AND (d.name LIKE CONCAT('%', #{q.keyword}, '%')
                OR d.phone LIKE CONCAT('%', #{q.keyword}, '%'))
            </if>
            <if test="q.departmentId != null">
                AND d.department_id = #{q.departmentId}
            </if>
            <if test="q.status != null">
                AND d.status = #{q.status}
            </if>
            <if test="q.auditStatus != null">
                AND da.audit_status = #{q.auditStatus}
            </if>
        </where>
    </select>

</mapper>